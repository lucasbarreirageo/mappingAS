% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{map_species}
\alias{map_species}
\title{Interactive map of a species' points, EOO and AOO (MapBiomas + fire)}
\usage{
map_species(
  assessment,
  species = NULL,
  mapbiomas = TRUE,
  fire = FALSE,
  src = NULL,
  fire_src = NULL,
  max_pixels = 800,
  lang = c("pt", "en"),
  clip = c("eoo", "aoo", "all"),
  protected = FALSE,
  pa_src = NULL,
  pa_occ_only = TRUE
)
}
\arguments{
\item{assessment}{A \code{geoconv_assessment} from \code{\link{assess_species}}.}

\item{species}{Species name to plot (default: first assessed).}

\item{mapbiomas}{Logical; overlay the MapBiomas raster (default \code{TRUE}).}

\item{fire}{Logical; overlay the MapBiomas Fire accumulated layer
(default \code{FALSE}).}

\item{src}{Optional MapBiomas LULC GeoTIFF path/URL.}

\item{fire_src}{Optional MapBiomas Fire GeoTIFF path/URL.}

\item{max_pixels}{Target maximum raster size (longest side, pixels) for the
overlays (default \code{800}).}

\item{lang}{Legend language: \code{"pt"} (default) or \code{"en"}. Controls
the language of the MapBiomas class labels and the fire-frequency legend.}

\item{clip}{Geometry the rasters are clipped to: \code{"eoo"} (default, the
EOO hull), \code{"aoo"} (the union of occupied AOO cells) or \code{"all"} (the
union of both, so the raster spans the whole range).}

\item{protected}{Logical; overlay protected areas. Uses the protected-area
layer already stored by \code{assess_species(..., protected = TRUE)}, or
fetches it on the fly from WDPA otherwise. Default \code{FALSE}.}

\item{pa_src}{Optional local protected-area vector file for the overlay
(offline).}

\item{pa_occ_only}{Logical; when \code{TRUE} (default) the map draws only the
UCs that actually contain occurrences of the species. Set \code{FALSE} to
draw every UC the range overlaps. Does not affect the EOO/AOO overlap
percentages, which are computed over the whole range.}
}
\value{
A \code{leaflet} widget.
}
\description{
Builds a \pkg{leaflet} map with the occurrence points, EOO hull and occupied
AOO cells. Optionally overlays the MapBiomas land-cover raster and the
MapBiomas Fire accumulated layer (fire recurrence: number of years burned),
both clipped to the EOO, as toggleable layers with legends.
}
