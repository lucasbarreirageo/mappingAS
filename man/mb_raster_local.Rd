% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbiomas_local.R
\name{mb_raster_local}
\alias{mb_raster_local}
\title{Crop a MapBiomas LULC raster to an area of interest (local backend)}
\usage{
mb_raster_local(aoi, year = 2024, collection = 10, src = NULL, mask = TRUE)
}
\arguments{
\item{aoi}{An sf/sfc polygon (any CRS) defining the area to extract (e.g. an EOO hull or the union of AOO cells).}

\item{year}{Integer year (default 2024).}

\item{collection}{Integer collection number (default 10).}

\item{src}{Optional path or URL to a MapBiomas GeoTIFF. If NULL (default) the public Collection URL for `year` is used through /vsicurl/.}

\item{mask}{Logical; if TRUE (default) pixels outside the polygon are set to NA. If FALSE only a rectangular crop is returned.}

}
\value{
A terra SpatRaster of MapBiomas pixel codes restricted to the AOI, in the raster's native CRS.
}
\description{
Reads the MapBiomas national mosaic and crops/masks it to `aoi` using terra. By default it streams a windowed read of only the AOI bounding box from the public GeoTIFF via GDAL /vsicurl/, so there is no Earth Engine account and no full-country download.
}
\seealso{
\code{\link{mb_class_areas_raster}}, \code{\link{mb_class_areas_gee}}
}
