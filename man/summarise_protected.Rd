% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protected_areas.R
\name{summarise_protected}
\alias{summarise_protected}
\title{Overlap between a species' range and federal Conservation Units}
\usage{
summarise_protected(points, pa, eoo = NULL, aoo = NULL)
}
\arguments{
\item{points}{An \code{sf} of POINT geometries (one species).}

\item{pa}{A UC \code{sf} from \code{\link[=protected_areas]{protected_areas()}} (standardised columns).}

\item{eoo, aoo}{The \code{eoo}/\code{aoo} sublists produced by \code{\link[=assess_species]{assess_species()}} (or
\code{\link[=calc_eoo]{calc_eoo()}}/\code{\link[=calc_aoo]{calc_aoo()}}). Either may be \code{NULL}.}
}
\value{
A list: \code{n_occ}, \code{n_occ_in}, \code{occ_pct}; \code{eoo_pct}, \code{eoo_km2};
\code{aoo_pct}, \code{aoo_km2}, \code{aoo_cells_in}; \code{n_uc}; \code{list} (per-UC data.frame)
and \code{layer} (the UC \code{sf}).
}
\description{
Given a species' occurrence points and (optionally) its EOO and AOO, plus a
UC layer from \code{\link[=protected_areas]{protected_areas()}}, computes how much of the range is legally
protected: the share of occurrences inside UCs, the percentage/area of the
EOO and AOO that fall within UCs, and the list of UCs touched.
}
\details{
Percentages are computed on the species' equal-area projection so ratios are
exact; the reported \code{eoo_km2}/\code{aoo_km2} inside UCs are derived from the
headline EOO/AOO so they stay consistent with \code{\link[=assess_species]{assess_species()}}. The AOO
overlap follows the grid nature of the metric: an occupied 2-km cell counts
as protected when it intersects any UC.
}
