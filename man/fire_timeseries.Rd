% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire.R
\name{fire_timeseries}
\alias{fire_timeseries}
\title{Burned-area time series for a polygon (\% of range x year)}
\usage{
fire_timeseries(
  geom,
  years = NULL,
  fire_collection = 4,
  host_collection = 9,
  range_km2 = NULL,
  verbose = TRUE
)
}
\arguments{
\item{geom}{An \code{sf}/\code{sfc} polygon.}

\item{years}{Integer vector (default \code{\link{mb_years}()}).}

\item{fire_collection}{Fire collection number (default \code{4}).}

\item{host_collection}{Initiative folder hosting the fire data
(default \code{9}; Fire Col.4 lives under \code{collection_9/}).}

\item{range_km2}{Range area for the percentage denominator (optional).}

\item{verbose}{Logical; print progress.}
}
\value{
A \code{data.frame} with \code{year}, \code{burned_km2},
\code{burned_pct}.
}
\description{
Fire analogue of \code{\link{cover_timeseries}}: reads the annual burned
layer for each year and returns burned area (km^2) and percentage of the
range. Each year is one windowed read, so a full annual series can be slow.
}
