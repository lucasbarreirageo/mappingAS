% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire.R
\name{fire_raster_local}
\alias{fire_raster_local}
\title{Crop a MapBiomas Fire raster to an area of interest (local backend)}
\usage{
fire_raster_local(
  aoi,
  year = 2024,
  product = c("accumulated", "annual"),
  fire_collection = 4,
  host_collection = 9,
  src = NULL,
  mask = TRUE,
  cache = TRUE,
  cache_dir = NULL
)
}
\arguments{
\item{aoi}{An \code{sf}/\code{sfc} polygon (e.g. an EOO hull or AOO union).}

\item{year}{Integer year (ignored when \code{product = "accumulated"}).}

\item{product}{\code{"accumulated"} (default) or \code{"annual"}.}

\item{fire_collection}{Fire collection number (default \code{4}).}

\item{host_collection}{Initiative folder hosting the fire data
(default \code{9}; Fire Col.4 lives under \code{collection_9/}).}

\item{src}{Optional GeoTIFF path/URL overriding the built fire URL.}

\item{mask, cache, cache_dir}{Passed to \code{\link{mb_raster_local}}.}
}
\value{
A \pkg{terra} \code{SpatRaster} of fire values clipped to the AOI.
}
\description{
Thin wrapper over \code{\link{mb_raster_local}}; reuses its windowed
\code{/vsicurl/} read and on-disk cache.
}
