% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/donut.R
\name{plot_conversion_donut}
\alias{plot_conversion_donut}
\title{MapBiomas composition donut charts (EOO and AOO)}
\usage{
plot_conversion_donut(
  assessment,
  species = NULL,
  by = c("class", "group"),
  lang = c("en", "pt")
)
}
\arguments{
\item{assessment}{A \code{geoconv_assessment} from \code{\link{assess_species}}
(run with \code{mapbiomas = TRUE}).}

\item{species}{Optional species name. \code{NULL} (default) uses the first
assessed species.}

\item{by}{\code{"class"} (default) for one ring slice per MapBiomas class, or
\code{"group"} for the Natural / Altered / Water / Other summary.}

\item{lang}{\code{"en"} (default) or \code{"pt"} for slice labels.}
}
\value{
A \pkg{ggplot} object (with mappingAS metadata attached in attribute
\code{"mas"} so \code{\link{mas_plotly}} can build the interactive donut).
Stops with an informative message if no class data are available.
}
\description{
Two side-by-side donut (ring) charts summarising the MapBiomas composition of
the Extent of Occurrence (EOO) and Area of Occupancy (AOO) of a species, in
the official MapBiomas colours. Slices can be individual land-cover classes
(\code{by = "class"}) or the four conservation groups
(\code{by = "group"}). Designed to be exported as a transparent-background
PNG (all panel/plot/legend backgrounds are transparent), and the interactive
\pkg{plotly} version (via \code{\link{mas_plotly}}) adds outside labels with
leader lines and a transparent export.
}
\examples{
\dontrun{
res <- assess_species(read_occurrences("occ.csv"))
plot_conversion_donut(res, by = "class")
mas_plotly(plot_conversion_donut(res, by = "group"))
}
}
\seealso{
\code{\link{mas_plotly}} for the interactive version;
\code{\link{plot_conversion}} for the stacked-bar summary.
}
